// import { Helmet } from "react-helmet"

// interface SEOProps {
//   title: string
//   description?: string
// }

// export function SEO({ title, description }: SEOProps) {
//   return (
//     <Helmet>
//       <title>{title}</title>
//       {description && <meta name="description" content={description} />}
//       <meta name="viewport" content="width=device-width, initial-scale=1" />
//     </Helmet>
//   )
// }


// TODO: Fix this react-helmet import issue. 
// It seems to be a problem with the library itself, not our code. For now, we can just set the title and meta tags directly in the index.html file or use a different library for managing the document head in React.